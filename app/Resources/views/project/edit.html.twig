{% extends 'base.html.twig' %}

{% block title %}
	Anlegg
{% endblock %}

{% block body %}

	{{ form_start(form) }}
	{% for url in form.vars.value.images %}
		<img src="{{ url }}" />
	{% endfor %}
	{{ form_widget(form) }}
	{{ form_end(form) }}

{% endblock %}


