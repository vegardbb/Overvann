{% extends 'base.html.twig' %}

{% block title %}
    Selskap
{% endblock %}

{% block body %}

    <div class="row">
        <div class="small-12   medium-6 medium-offset-3  large-6 large-offset-3 columns">
            <p></p>

            <p>{{ project.getId() }}</p>
            <p>{{ project.getName() }}</p>
            <p>{{ project.getType () }}</p>

        </div>
    </div>

{% endblock %}

