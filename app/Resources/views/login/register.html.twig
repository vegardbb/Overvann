{# app/Resources/views/login/register.html.twig #}

{% extends 'base.html.twig' %}

{% block body %}
	{% if form is defined %}
		{{ form_start(form) }}
		{{ form_widget(form) }}
		{{ form_end(form) }}
    {% endif %}
{% endblock %}
